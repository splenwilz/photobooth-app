# CLAUDE.md

## Development Approach

- Always use Test-Driven Development (TDD) when developing features: write failing tests first (RED), implement code to make them pass (GREEN), then refactor (REFACTOR).
